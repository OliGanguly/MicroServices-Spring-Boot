server:
  port: 8081

#go and connect with eureka service-registry
eureka:
  client:
    service-url:
      defaultZone : http://localhost:8761/eureka/

management:
  tracing:
    sampling:
      probability: 1.0
